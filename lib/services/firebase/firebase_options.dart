// PLACEHOLDER — generated files like this should come from running:
//   flutterfire configure
// against YOUR OWN Firebase project. Do not commit real API keys for a
// project you don't control; this stub lets the app compile and clearly
// fails fast with a helpful message if Firebase.initializeApp is called
// without replacing it.
//
// See README → "Firebase Setup" for the exact steps.

import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'DefaultFirebaseOptions has not been configured for this project yet.\n'
      'Run `flutterfire configure` with your own Firebase project to '
      'generate a real lib/services/firebase/firebase_options.dart, '
      'then remove this placeholder.',
    );
  }
}
